# คู่มือการสำรองและกู้คืนข้อมูล (Backup & Recovery Guide)

เอกสารนี้อธิบายวิธีการสำรองข้อมูล (Backup) และกู้คืนข้อมูล (Restore) สำหรับระบบ MicroFormula ทั้งในกรณีใช้งานผ่าน Cloud (Supabase) และ Local Database

---

## 1. กรณีใช้งาน Local Database (Docker) 🏠

หากคุณรันระบบบนเครื่องตัวเองโดยใช้ Docker ข้อมูลจะถูกเก็บอยู่ใน Docker Volume การสำรองข้อมูลทำได้ง่ายๆ ด้วยสคริปต์ที่เราเตรียมไว้ให้

### การสำรองข้อมูล (Backup)
1. เปิด Terminal ในโฟลเดอร์ของโปรเจกต์
2. รันคำสั่ง:
   ```bash
   ./scripts/backup-local-db.sh
   ```
3. ระบบจะสร้างไฟล์ `.sql` ที่ระบุวันที่และเวลาไว้ในโฟลเดอร์ `backups/` เช่น `backups/backup_2025-12-31_12-00-00.sql`

> **แนะนำ:** ควรตั้งเวลาให้รันสคริปต์นี้ทุกวัน (Automated Cron Job) หรือกดรันเองทุกครั้งที่มีการปิดยอดบัญชีสำคัญ

### การกู้คืนข้อมูล (Restore)
กรณีเครื่องพัง หรือต้องการย้อนกลับไปยังจุดก่อนหน้า
1. เลือกไฟล์ Backup ที่ต้องการจากโฟลเดอร์ `backups/`
2. รันคำสั่ง (แทนที่ `<path_to_file>` ด้วยชื่อไฟล์จริง):
   ```bash
   ./scripts/restore-local-db.sh backups/backup_2025-xxx.sql
   ```
3. พิมพ์ `y` เพื่อยืนยัน
4. รอจนกว่าระบบจะแจ้งว่า **Restoration successful!**

---

## 2. กรณีใช้งาน Supabase Cloud ☁️

หากคุณใช้งานผ่าน Supabase Cloud ระบบจะมีระบบ Backup อัตโนมัติอยู่แล้ว แต่คุณสามารถทำ Manual Backup ได้ดังนี้

### การสำรองข้อมูล (Backup)
1. เข้าไปที่ [Supabase Dashboard](https://supabase.com/dashboard)
2. เลือก Project ของคุณ
3. ไปที่เมนู **Database** -> **Backups**
4. คุณสามารถดาวน์โหลดไฟล์สำรองข้อมูลได้จากหน้านี้
5. หรือใช้ CLI: `supabase db dump -f supabase.sql`

### การกู้คืนข้อมูล (Recovery)
- ในหน้า **Database** -> **Backups** คุณสามารถกด **Restore** เพื่อย้อนข้อมูลกลับไปยังช่วงเวลาที่ต้องการได้ (Point-in-Time Recovery - PITR) ขึ้นอยู่กับแพ็กเกจที่คุณใช้งาน

---

## 3. แผนการอัปเดตเวอร์ชัน (Update Plan) 🔄

เมื่อมีเวอร์ชันใหม่ของ MicroFormula ออกมา ลูกค้าสามารถอัปเดตได้ดังนี้:

### กรณีใช้งาน Docker (แนะนำ)
1. ดึงโค้ดเวอร์ชันล่าสุด:
   ```bash
   git pull origin main
   ```
2. อัปเดต Container:
   ```bash
   docker-compose pull
   docker-compose up -d --build
   ```
3. ระบบจะทำงานต่อด้วยโค้ดใหม่และข้อมูลเดิม (Database ไม่หาย)

### กรณีรัน Manual (npm run dev)
1. หยุดการทำงานของ Server (Ctrl+C)
2. ดึงโค้ดล่าสุด: `git pull`
3. ลง Dependencies ใหม่ (เผื่อมี): `npm install`
4. รันใหม่: `npm run dev`

---

## สรุป (Summary)
- **ข้อมูลสำคัญที่สุดคือ Database**: ไฟล์โค้ดโปรแกรมหายสามารถโหลดใหม่ได้ แต่ Database หายคือหายเลย
- **ความถี่ในการ Backup**: แนะนำวันละ 1 ครั้ง หรือทันทีหลังปิดงบเดือน
- **การทดสอบ**: ควรลองกู้คืนข้อมูล (Restore) ในเครื่องสำรองปีละ 1-2 ครั้ง เพื่อมั่นใจว่าไฟล์ Backup ใช้งานได้จริง
