# ⚙️ การตั้งค่าระบบ (System Setup)

การตั้งค่าที่ถูกต้องคือพื้นฐานของระบบบัญชีที่ดี หน้านี้จะแนะนำขั้นตอนการเตรียมพร้อมก่อนเริ่มทำงานจริง

---

## 1. การติดตั้งและเริ่มต้นใช้งาน (Installation) 🚀
ระบบ MicroFormula ออกแบบมาให้ติดตั้งง่ายที่สุดด้วยระบบ **One-Click Installation** ผ่านเทคโนโลยี Docker:

### ⚡ วิธีเริ่มต้นใช้งานแบบ One-Click (รวดเร็วที่สุด)
1. **เตรียมความพร้อม**: ติดตั้ง [Docker Desktop](https://www.docker.com/products/docker-desktop/) ไว้ในเครื่อง
2. **รันคำสั่งเดียวจบ**:
   - **Windows**: ดับเบิลคลิกที่ไฟล์ `START_WINDOWS.bat`
   - **Linux / Ubuntu**: รันคำสั่ง `sh START_LINUX.sh`
3. **ระบบจะทำงานให้อัตโนมัติ**: 
   - ตรวจสอบความถูกต้องของระบบ
   - เริ่มต้นฐานข้อมูล (Database) และตัวโปรแกรม (App)
   - เปิด Browser ไปที่ [http://localhost:3000](http://localhost:3000) ให้ทันที

> **หมายเหตุ**: การรันครั้งแรกอาจใช้เวลา 1-3 นาทีขึ้นอยู่กับความเร็วอินเทอร์เน็ตในการดึงข้อมูลระบบครั้งแรกครับ หลังจากนั้นจะทำงานทันทีในครั้งต่อไป

---

### 🐳 สำหรับสาย Docker (Unified Management)
หากคุณต้องการรันผ่าน Command Line โดยตรง สามารถเข้าไปที่โฟลเดอร์หลัก (`app-micro-account`) แล้วใช้คำสั่ง:
```bash
docker-compose up -d
```
คำสั่งนี้จะทำการเปิดระบบทั้งหมด (App, DB, pgAdmin) ขึ้นมาพร้อมกันในโหมด Background ครับ

---

## 2. การเปิดใช้งานลิขสิทธิ์ (License Key Activation) 🔑
เมื่อคุณเข้าใช้งานครั้งแรก ระบบจะแจ้งเตือนให้กรอก **License Key** (รหัสเปิดใช้งาน):
1. **ขอรับคีย์**: ติดต่อผู้พัฒนาเพื่อรับคีย์ตามความต้องการ (รายเดือน / รายปี / ตลอดชีพ)
2. **กรอกคีย์**: นำคีย์ที่ได้รับมาวางในหน้าจอล็อคแล้วกด "ยืนยัน"
3. **ใช้งานได้ทันที**: ระบบจะตรวจสอบความถูกต้องและเปิดให้คุณเข้าถึงฟีเจอร์ทั้งหมด

---

## 3. การเข้าสู่ระบบ (Login)
หลังจากเปิดใช้งานคีย์แล้ว คุณสามารถเข้าสู่ระบบด้วยบัญชีเริ่มต้น:
- **Email**: `admin@local`
- **Password**: *(กรุณาตรวจสอบจากผู้ดูแลระบบหรือตั้งค่าใหม่ในระบบ Local)*

## 2. การตั้งข้อมูลบริษัท (Company Identity) 🏆
เป็นส่วนที่สำคัญที่สุด เพราะข้อมูลนี้จะปรากฏในเอกสารทุกฉบับ
1.  ไปที่เมนู **การตั้งค่า (Settings)** จากแถบเมนูด้านซ้าย
2.  **อัปโหลดโลโก้**: แนะนำไฟล์ PNG พื้นหลังโปร่งใสเพื่อให้ดูสวยงามบนใบแจ้งหนี้
3.  **รายละเอียดการติดต่อ**: ระบุชื่อบริษัท, ที่อยู่, เบอร์โทร และ Email ให้ครบถ้วน
4.  **Tax ID**: เลขประจำตัวผู้เสียภาษี 13 หลัก (สำคัญมากสำหรับการออกใบกำกับภาษี)

## 3. การจัดการสิทธิ์ผู้ใช้งาน (Roles & Permissions)
ระบบมีระบบจัดการสิทธิ์ (Role-Based Access Control) เพื่อความปลอดภัย:
- **Admin**: เข้าถึงได้ทุกเมนู รวมถึงการตั้งค่าระบบและการลบข้อมูลสำคัญ
- **User (Accountant)**: เน้นการทำงานเอกสารและดูรายงาน
- **Viewer**: สามารถดูข้อมูลได้อย่างเดียว (Read-only)

---

## ❓ คำถามที่พบบ่อย (FAQ)
**Q: หากต้องการเปลี่ยนชื่อบริษัทในภายหลังจะมีผลต่อเอกสารเก่าไหม?**
*A: ระบบจะอ้างอิงข้อมูลปัจจุบันเมื่อมีการสั่งพิมพ์ หากต้องการให้เอกสารเก่าคงเดิม แนะนำให้พิมพ์เป็น PDF เก็บไว้ก่อนทำการแก้ไข*

**Q: สามารถใช้งานบนมือถือได้ไหม?**
*A: ได้ครับ ระบบถูกออกแบบมาให้เป็น Responsive Design สามารถตรวจสอบสต็อกหรือดู Dashboard ผ่านมือถือได้อย่างสะดวก*
